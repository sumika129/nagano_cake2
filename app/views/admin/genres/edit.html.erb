<h3>ジャンル編集</h3>

<%= form_with model: [:admin,@genre] do |f| %>
<table>
  <tr>
    <th>ジャンル名</th>
    <td><%= f.text_field :name %></td>
    <td><%= f.submit '変更を保存' %></td>
  </tr>
</table>
<% end %>
